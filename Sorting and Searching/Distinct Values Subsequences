/*
    Author: kamine
    Created: 2025-09-05 09:47:27
*/
#include <bits/stdc++.h>

using namespace std;

vector<int> compress(vector<int> a) {
    vector<int> vals = a;
    sort(vals.begin(), vals.end());
    vals.erase(unique(vals.begin(), vals.end()), vals.end());
    vector<int> res(a.size());
    for (int i = 0; i < a.size(); ++i) {
        res[i] = lower_bound(vals.begin(), vals.end(), a[i]) - vals.begin();
    }
    return res;
}

const int MOD = 1e9 + 7;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<int> a(n);
    for (int& x : a) {
        cin >> x;
    }    

    a = compress(a);
    vector<int> cnt(n);
    for (int x : a) {
        cnt[x]++;
    }

    int64_t ans = 1;
    for (int i = 0; i < n; i++) {
        if (cnt[i]) {
            ans = (ans * (cnt[i] + 1)) % MOD;
        }
    }

    cout << ans - 1;
}
